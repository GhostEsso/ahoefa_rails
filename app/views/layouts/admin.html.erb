<!DOCTYPE html>
<html>
  <head>
    <title>Administration - Ahoefa</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <div class="min-h-screen flex">
      <!-- Sidebar -->
      <div class="bg-indigo-800 w-64 flex flex-col fixed h-full">
        <div class="flex items-center justify-center h-16 bg-indigo-900">
          <%= link_to admin_root_path, class: "text-white text-2xl font-bold" do %>
            Administration
          <% end %>
        </div>
        <nav class="flex-1 px-2 py-4 space-y-2">
          <%= link_to admin_root_path, class: "flex items-center px-4 py-2 text-gray-100 hover:bg-indigo-700 rounded-lg #{current_page?(admin_root_path) ? 'bg-indigo-700' : ''}" do %>
            <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
            Tableau de bord
          <% end %>
          <%= link_to admin_agents_path, class: "flex items-center px-4 py-2 text-gray-100 hover:bg-indigo-700 rounded-lg #{controller_name == 'agents' ? 'bg-indigo-700' : ''}" do %>
            <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
            </svg>
            Agents
          <% end %>
        </nav>
        <div class="p-4 border-t border-indigo-700">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="h-8 w-8 rounded-full bg-indigo-600 flex items-center justify-center">
                <span class="text-white font-semibold">
                  <%= current_user.first_name[0] %><%= current_user.last_name[0] %>
                </span>
              </div>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-white">
                <%= current_user.full_name %>
              </p>
              <%= button_to destroy_user_session_path, method: :delete, class: "text-xs text-indigo-300 hover:text-white" do %>
                Se déconnecter
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <!-- Main content -->
      <div class="flex-1 ml-64">
        <header class="bg-white shadow">
          <div class="px-4 py-6 sm:px-6 lg:px-8">
            <h1 class="text-2xl font-bold text-gray-900">
              <%= yield(:title) if content_for?(:title) %>
            </h1>
          </div>
        </header>
        <main class="px-4 py-8 sm:px-6 lg:px-8">
          <%= render 'shared/flash' if flash.any? %>
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html> 